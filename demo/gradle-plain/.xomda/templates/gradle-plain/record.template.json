{
  "uuid": "88b234c0-b2ea-4cf9-952a-530acb89715e",
  "name": "Java record",
  "description": "Generates an immutable Java record per entity (under src/main/generated/...). Same logic as demo/maven-plain's record template — duplicated rather than shared so each demo owns its templates.",
  "version": "1.0.0",
  "folder": "gradle-plain",
  "cells": [
    {
      "uuid": "891f20c9-e2d8-40a8-8c30-33346d2053ba",
      "type": "loop",
      "content": "",
      "variableName": "$entity",
      "loopSource": "entities",
      "children": [
        {
          "uuid": "a95389e1-d946-4dfc-bb37-07107922a001",
          "type": "logic",
          "content": "const javaPrim = { string:'String', number:'Long', boolean:'Boolean', decimal:'java.math.BigDecimal', date:'java.time.LocalDateTime', uuid:'java.util.UUID' };\nisPrim = function (t) { return Object.prototype.hasOwnProperty.call(javaPrim, t); };\njavaShort = function (fq) { const dot = fq.lastIndexOf('.'); return dot >= 0 ? fq.slice(dot + 1) : fq; };\n// aggregation === 'none' → stored as the referent's UUID, not embedded.\nisRef = function (a) { return a.aggregation === 'none'; };\njavaTypeOf = function (a) {\n  if (isRef(a) && !isPrim(a.type)) return 'UUID';\n  if (isPrim(a.type)) return javaShort(javaPrim[a.type]);\n  return pascalCase(a.type);\n};\nfqOf = function (a) {\n  if (isRef(a) && !isPrim(a.type)) return 'java.util.UUID';\n  if (isPrim(a.type) && javaPrim[a.type].includes('.')) return javaPrim[a.type];\n  return null;\n};\nfields = (attributes || []).map(function (a) { return { camel: camelCase(a.name), javaType: javaTypeOf(a), fq: fqOf(a) }; });\nconst importSet = new Set();\nfor (const f of fields) { if (f.fq) importSet.add(f.fq); }\nimportLines = Array.from(importSet).sort().map(function (i) { return 'import ' + i + ';'; }).join('\\n');\npkgPath = ($packages && $packages.length ? $packages.map(function (p) { return p.name; }).join('.') : 'com.example.model');\nrecordParams = fields.map(function (f) { return '    ' + f.javaType + ' ' + f.camel; }).join(',\\n');\n"
        },
        {
          "uuid": "68a34a41-7b63-49a7-b724-9c002f8b2686",
          "type": "handlebars",
          "content": "package {{pkgPath}};\n\n{{{importLines}}}\n\n/**\n * Generated by xomda. Do not edit by hand — re-run `pnpm generate`.\n */\npublic record {{pascalCase name}}(\n{{{recordParams}}}\n) {}\n"
        },
        {
          "uuid": "a12ac590-51d9-4ad9-af01-a7efef225260",
          "type": "output",
          "content": "",
          "outputType": "file",
          "outputFilename": "src/main/generated/com/example/model/{{pascalCase name}}.java"
        }
      ]
    }
  ]
}
