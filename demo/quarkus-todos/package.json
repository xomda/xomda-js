{
  "name": "@xomda/demo-quarkus-todos",
  "version": "0.0.3",
  "private": true,
  "description": "xomda Quarkus 3 demo — real, runnable Todo service whose Panache entities, JAX-RS resources, enums and Flyway migration are generated by xomda into src/main/generated{,-resources}/.",
  "type": "module",
  "scripts": {
    "model": "tsx scripts/build-model.ts",
    "generate": "pnpm model && xomda generate",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:jvm": "mvn test"
  },
  "dependencies": {
    "xomda": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "25.9.2",
    "tsx": "4.23.11",
    "vitest": "4.1.8"
  }
}
