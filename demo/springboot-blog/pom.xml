<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.0.0</version>
    <relativePath/>
  </parent>

  <groupId>com.example</groupId>
  <artifactId>xomda-demo-springboot-blog</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>BlogService</name>
  <description>
    xomda Spring Boot 4 demo — a real, runnable Blog service. Hand-written
    application skeleton (this pom, application.yml, BlogServiceApplication,
    docker-compose.yml). The JPA entities, DTOs, repositories, services,
    enums and Flyway V1 migration are generated by xomda into
    src/main/generated/ and src/main/generated-resources/, wired in via
    build-helper-maven-plugin so `mvn test` compiles and exercises both halves.
  </description>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!--
      Spring Data Elasticsearch — bare dependency, NO starter. The xomda
      `elasticsearch-document.template.json` emits `@Document` POJOs under
      `src/main/generated/com/example/blog/search/`; this dep makes them
      compile. We deliberately don't include
      `spring-boot-starter-data-elasticsearch` — that would try to autowire
      an ES client at startup and the test profile has no Elasticsearch
      backend. Pure compile-time code-gen demo.
    -->
    <dependency>
      <groupId>org.springframework.data</groupId>
      <artifactId>spring-data-elasticsearch</artifactId>
      <exclusions>
        <exclusion>
          <groupId>org.elasticsearch.client</groupId>
          <artifactId>elasticsearch-rest-client</artifactId>
        </exclusion>
        <exclusion>
          <groupId>co.elastic.clients</groupId>
          <artifactId>elasticsearch-java</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <!-- Test stack: Spring Boot test + H2 so `mvn test` runs without Docker. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <!--
      Standard hand-written resources + xomda-generated resources (the Flyway
      V1__init.sql migration). Generated resources live in their own root so
      they can be gitignored without touching hand-written application.yml.
    -->
    <resources>
      <resource>
        <directory>${project.basedir}/src/main/resources</directory>
      </resource>
      <resource>
        <directory>${project.basedir}/src/main/generated-resources</directory>
      </resource>
    </resources>

    <plugins>
      <!--
        Wire `src/main/generated/` (xomda-produced Java) into the Maven
        source roots so `mvn compile` picks up entities/repositories/services
        alongside hand-written code. Per the layout convention: generated
        under `generated/`, hand-written under `main/java/`, tests under
        `test/java/` — never overlapping.
      -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <version>3.6.0</version>
        <executions>
          <execution>
            <id>add-generated-sources</id>
            <phase>generate-sources</phase>
            <goals>
              <goal>add-source</goal>
            </goals>
            <configuration>
              <sources>
                <source>${project.basedir}/src/main/generated</source>
              </sources>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
