{
  "name": "@xomda/demo-springboot-blog",
  "version": "0.0.2",
  "private": true,
  "description": "xomda Spring Boot 4 demo — real, runnable Blog service whose JPA entities, repositories, services, DTOs, enums and Flyway migration are generated by xomda into src/main/generated{,-resources}/.",
  "type": "module",
  "scripts": {
    "model": "tsx scripts/build-model.ts",
    "generate": "pnpm model && xomda generate",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:jvm": "mvn test"
  },
  "dependencies": {
    "xomda": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "25.9.1",
    "tsx": "4.22.3",
    "vitest": "4.1.8"
  }
}
