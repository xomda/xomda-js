// Side-effect imports of every built-in module. Importing this file wires
// every module's route + nav + setup contribution into the registry.
// Adding a built-in module = create the file + add one import line here.
import './home'
import './model'
import './versions'
import './templates'
import './generate'
import './files'
import './logs'
import './settings'
